# Diet Agent 用户提问处理流程图

> 一张图看懂：用户提问后系统如何判断意图、进入哪条流程，以及每一步对应的代码文件。
> 入口：`POST /api/v1/diet/chat` → `DietChatController.dietChat` → `DietOrchestratorService.dietChat`

```mermaid
flowchart TD
    A([用户发起提问]) --> B["HTTP 入口<br/>DietChatController.java<br/><i>POST /api/v1/diet/chat</i>"]

    B --> C["编排器入口 dietChat()<br/>DietOrchestratorService.java<br/><i>生成 traceId · 校验参数</i>"]

    C --> D{"加载/创建会话状态<br/>loadOrCreate()<br/>SessionStateService.java"}

    D --> E["记录用户消息<br/>SessionService.java → diet_messages"]
    E --> F{"PERSONAL 模式<br/>且个人餐食库为空?"}
    F -- "是" --> F1["返回引导文案<br/><i>completeTextOnly()</i>"]
    F -- "否" --> G["意图识别 recognize()<br/>IntentAgentService.java<br/><i>调用 IntentAgent(qwen-turbo)</i>"]

    G --> H["意图矫正 revise()<br/>IntentReviseService.java<br/><i>结合历史 phase/slots 修正</i>"]

    H --> I{"意图路由 switch(intent)<br/>Intent 枚举"}

    I -->|"MEAL_RECOMMENDATION / CLARIFY_NEEDED"| R["推荐主链路 handleRecommendation()"]

    I -->|"MEAL_ADJUST"| AD["调整链路 handleAdjust()"]

    I -->|"MEAL_PLAN"| PL["多餐规划 handlePlan()"]

    I -->|"HEALTH_RISK"| HR["健康风险 handleHealthRisk()"]

    I -->|"OTHER"| CH["闲聊 handleChitchat()"]

    subgraph 推荐主链路
        R --> R1["槽位合并 merge()<br/>SlotMergeService.java"]
        R1 --> R2["澄清判断 decide()<br/>ClarifyAgentService.java<br/><i>规则层+ClarifyAgent</i>"]
        R2 --> R2D{"action == ASK ?<br/><i>槽位是否充足</i>"}
        R2D -- "是(缺槽位)" --> R2A["返回追问 completeAsk()<br/><i>phase=CLARIFY</i>"]
        R2D -- "否(槽位够)" --> R3["检索 search()<br/>MealSearchService.java"]
        R3 --> R4["重排 rank()<br/>MealRankService.java"]
        R4 --> R4D{"ranked 为空?"}
        R4D -- "是" --> R4A["空结果提示 completeTextOnly()"]
        R4D -- "否" --> R5["生成理由与回复 recommendAndRespond()<br/>RecommendResponseAgentService.java<br/><i>RecommendResponseAgent(qwen-max)</i>"]
        R5 --> R6["风险审查 check()<br/>RiskGuardService.java"]
        R6 --> R6D{"guard.passed() ?"}
        R6D -- "否" --> R6A["改写为保守提示"]
        R6D -- "是" --> R7["保存状态+落库<br/>SessionStateService / SessionService"]
        R7 --> OUT["返回 ChatResponse(推荐卡片)"]
    end

    subgraph 调整链路
        AD --> AD1["槽位合并 merge()"]
        AD1 --> AD2["取已推荐 excludeMealIds<br/><i>lastRecommendations</i>"]
        AD2 --> AD3["重跑推荐流水线<br/>completeRecommendation()<br/><i>换一批</i>"]
    end

    subgraph 多餐规划
        PL --> PL1["槽位合并 merge()"]
        PL1 --> PL2["解析餐次 resolveMealTimes()<br/>MealPlanService.java"]
        PL2 --> PL3["按餐次检索重排 planMeals()<br/>MealPlanService.java"]
        PL3 --> PL4{"有匹配?"}
        PL4 -- "否" --> PL4A["空方案提示"]
        PL4 -- "是" --> PL5["规划应答 planAndRespond()<br/>PlanResponseAgentService.java"]
        PL5 --> PL6["风险审查 RiskGuardService.java"]
        PL6 --> PL7["保存状态+落库"]
        PL7 --> OUT2["返回 ChatResponse(按餐次卡片)"]
    end

    subgraph 健康风险分支
        HR --> HR1["保守提示 conservativeMessage()<br/>RiskGuardService.java"]
        HR1 --> HR2["completeTextOnly()"]
    end

    subgraph 闲聊分支
        CH --> CH1["固定引导文案 CHITCHAT_REPLY"]
        CH1 --> CH2["completeTextOnly()"]
    end

    style B fill:#e3f2fd,stroke:#1976d2
    style C fill:#e3f2fd,stroke:#1976d2
    style I fill:#fff3e0,stroke:#f57c00
    style R2D fill:#fff3e0,stroke:#f57c00
    style R4D fill:#fff3e0,stroke:#f57c00
    style R6D fill:#fff3e0,stroke:#f57c00
    style OUT fill:#e8f5e9,stroke:#388e3c
    style OUT2 fill:#e8f5e9,stroke:#388e3c
```

## 各流程对应文件速查

| 流程/步骤 | 文件 |
|-----------|------|
| HTTP 入口 | `controller/chat/DietChatController.java` |
| 编排器（状态机总调度） | `service/orchestrator/DietOrchestratorService.java` |
| 会话状态加载/保存 | `service/session/SessionStateService.java` |
| 消息落库 | `service/session/SessionService.java` |
| 意图识别 | `service/intent/IntentAgentService.java` |
| 意图矫正 | `service/intent/IntentReviseService.java` |
| 槽位合并 | `service/slot/SlotMergeService.java` |
| 澄清追问 | `service/clarify/ClarifyAgentService.java` |
| 餐食检索 | `service/meal/MealSearchService.java` |
| 餐食重排 | `service/meal/MealRankService.java` |
| 推荐应答（理由+口语） | `service/recommend/RecommendResponseAgentService.java` |
| 多餐规划 | `service/plan/MealPlanService.java` |
| 规划应答 | `service/plan/PlanResponseAgentService.java` |
| 健康风险守卫 | `service/risk/RiskGuardService.java` |
| 链路追踪 | `service/trace/AgentTraceService.java` |
| Agent 构建器 | `agent/builder/*AgentBuilder.java` |
| 意图枚举 | `enums/Intent.java` |

## 六种意图路由（`enums/Intent.java`）

| 意图 | 含义 | 处理分支 |
|------|------|----------|
| `MEAL_RECOMMENDATION` | 餐食推荐 | 推荐主链路 |
| `CLARIFY_NEEDED` | 需要澄清 | 推荐主链路（内部走澄清） |
| `MEAL_ADJUST` | 换一批/调整 | 调整链路 |
| `MEAL_PLAN` | 多餐规划 | 规划链路 |
| `HEALTH_RISK` | 健康风险咨询 | 保守提示 |
| `OTHER` | 闲聊/无关 | 固定引导 |
